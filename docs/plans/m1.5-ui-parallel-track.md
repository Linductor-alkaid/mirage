# M1.5：UI 并行轨道与 IPC 事件订阅

> 状态：Planned
> 负责人：Mirage 维护者
> 所属计划：[Mirage 实施总计划](mirage-implementation-plan.md)
> 前置：M1（已完成，退出条件复核 5/5 通过）
> 建议发布点：无独立 tag（能力增量随 master 开发线交付；产品化 UI 与分发仍属 M5）
> 更新日期：2026-09-18

## 目标

让产品 UI（`ui/`，Web 技术栈，见 [DEC-006](../decisions/DEC-006-ui-web-frontend-packaging.md)）
与 runtime 核心并行开发：以 Local IPC 契约为唯一耦合面，前端在浏览器中（mock 数据或经
dev bridge 连接真实 Service）直观验证 M1 已交付的任务能力，用于尽早判断产品形态是否
符合需求；同时把总计划 `EXEC-02` 的事件推送面落成 wire 契约
（[DEC-012](../decisions/DEC-012-ipc-event-subscription-and-wire-schema.md)）。

## 范围与非目标

范围：

- 协议 v1 wire schema 事实源文档与 TypeScript 镜像（含 golden vectors 双端一致性测试）。
- 协议 v1 事件订阅扩展（`events.subscribe` / `event` 信封 / M1.5 事件集）与 service 侧
  `executor::comm` 承载。
- 开发期 dev bridge（socket ↔ WebSocket 转发工具）。
- `ui/` 浏览器形态前端（mock 与真实双传输）：按
  [DEC-013](../decisions/DEC-013-frontend-ia-harness-first.md) 的 harness 优先统一壳
  组织——会话（chat）默认落地、workflow 一级入口、会话历史管理与统一设置；
  `M1.5-04` 交付的三视图是其最小闭环。
- CI 接线（前端一致性测试与构建不破坏既有预设矩阵）。

非目标：

- CEF 壳选型、PoC 与窗口集成（M3，DEC-006 决策 3）。
- Overlay、权限确认流 UI、Workflow 编排视图的产品化（M5，`SCOPE-06`）。
- DesktopObservation / Semantic Snapshot 事件（M2，DEC-005 待建；仅保留事件帧扩展点）。
- Windows 传输（M4）、发布打包、安装器与前端工具链定案（M5，DEC-006 决策 6）。
- 任何脱离 IPC 契约的前端-runtime 耦合（`RULE-01`：`apps/ui -> runtime` 仅经 IPC）。

## 设计与决策依据

- [DEC-006](../decisions/DEC-006-ui-web-frontend-packaging.md)：UI 技术路线与唯一耦合面。
- [DEC-007](../decisions/DEC-007-local-ipc-and-runtime-service.md)：传输、帧格式、协议 v1
  请求面与单连接单未决请求纪律。
- [DEC-012](../decisions/DEC-012-ipc-event-subscription-and-wire-schema.md)
  （Accepted，2026-09-16 评审通过）：事件订阅契约与 wire schema 事实源。原"契约类
  工作项（`M1.5-01`、`M1.5-02`）以其评审接受为前置"的门禁已解除。
- [DEC-013](../decisions/DEC-013-frontend-ia-harness-first.md)（Accepted）：
  前端信息架构（harness 优先统一壳）与设计规范，承载于
  [《Mirage 前端设计规范与信息架构》](../design/Mirage%20%E5%89%8D%E7%AB%AF%E8%AE%BE%E8%AE%A1%E8%A7%84%E8%8C%83%E4%B8%8E%E4%BF%A1%E6%81%AF%E6%9E%B6%E6%9E%84.md)；
  `M1.5-07`、`M1.5-08` 与 `M1.5-04` 视图迁移以其为验收基线。
- 总计划：`EXEC-01`/`EXEC-02`（Executor owner 与事件分发映射）、`RULE-01`（依赖方向）、
  `RULE-07`（背压显式化）。
- pinned 依据：`third_party/mira/third_party/executor`（`Topic` / `LatestMailbox` /
  `MpscChannel` 公开头与 communication capability card）。

## 工作项

- [x] `M1.5-01` 提供协议 v1 wire schema 事实源与双端一致性门禁：新建
  `docs/design/mirage-ipc-protocol-v1.md`（帧格式、请求/响应信封、消息表、错误码，
  与 `runtime/ipc` 现状互审一致），提取共享 golden vectors
  （`tests/runtime/data/ipc_protocol_golden.json`），新增 `ui/contracts` TypeScript
  镜像（消息类型 + 编解码 + 按 golden vectors 的一致性测试）。验收：schema 文档评审
  通过；C++ 与 TS 测试消费同一 vectors 全绿。
- [x] `M1.5-02` 提供 service 事件订阅能力并冻结事件帧语义：按 DEC-012 决策 2-5 实现
  `events.subscribe` / `events.unsubscribe`、`event` 信封（`v`/`seq`/`event`）、
  `task.updated` / `host.status` / `events.overflow` 事件集、hello `events` 能力通告、
  `executor::comm::Topic` + 每连接有界队列（drop-oldest）+ 响应优先写出、断连清理。
  验收：DEC-012 验证方式列出的协议/服务测试全部通过，M1 既有协议测试零回归。
- [x] `M1.5-03` 提供开发期 dev bridge 工具：`apps/devbridge` 将 Unix domain socket 上的
  协议帧透传为 WebSocket 帧（双向、保序、有界缓冲、显式关闭），进程内 Executor 纪律
  完整适用。验收：浏览器经 WebSocket 对真实 `mirage-service` 完成 hello 与
  task submit/inspect round-trip；工具可被 SIGINT 干净关闭。2026-09-18 完成：
  单 poll 循环 `DevBridgeLoop`（blocking I/O worker）承载全部会话，每浏览器会话
  1:1 连接上游，一条 WebSocket binary 消息恰承载一个完整帧（载荷逐字节透传），
  自实现 RFC 6455 服务端编解码，零新增第三方依赖；验收证据
  `docs/verification/m1.5-03-browser-e2e.png`。
- [x] `M1.5-04` 提供前端工程骨架与 mock 全流程可视化：`ui/` 初始化 Web 工程（开发期
  暂定 Vite + TypeScript，组件框架在实现评审时定选，均为暂定默认值、不构成 DEC-006
  决策 6 的定案）；基于 `ui/contracts` 实现 mock transport（内存任务状态机 + 事件
  模拟）；交付 Workspace（服务/主机状态、任务提交）与 Tasks/Execution（任务列表、
  step 级进度、结果与错误视图）。验收：浏览器中以 mock 数据完成 submit → step 级
  实时进度 → cancel → 终态的全流程可视化，无任何后端依赖。
- [ ] `M1.5-05` 提供前端对真实 Service 的联调与降级路径：经 dev bridge 连接真实
  `mirage-service`，事件订阅实时刷新；订阅不可用（`unsupported` / hello 无
  `events` 能力）时自动降级为 `task.inspect` 轮询；事件 `seq` 跳跃或 overflow 触发
  resync。验收：真实 submit → 实时进度 → cancel → 终态闭环在 UI 可见；降级路径在
  人为关闭订阅时行为正确。
- [x] `M1.5-06` 将前端门禁接入 CI：TS 一致性测试、前端构建与 lint 以独立 job 进入
  GitHub Actions，不破坏既有 CMake 预设矩阵；golden vectors 保持单一事实源（C++ 与
  TS 测试读取同一文件）。验收：CI 全绿且包含前端 job。
- [x] `M1.5-07` 按 DEC-013 落地统一壳与信息架构骨架：ActivityBar（会话/工作流/资源
  占位/设置）+ StatusBar（主机态、运行摘要、模型、紧急停止位）+ 路由表（设计规范
  3.2）+ 会话侧栏骨架（分组/搜索/四件套，数据经 mock）；M1.5-04 已交付视图按设计
  规范 3.3 迁移（Workspace 表单 → Composer 执行模式、Tasks → 会话侧栏、Execution
  → 运行抽屉/运行详情），功能等价不回退。验收：浏览器新 IA 下完成 M1.5-04 同款
  mock 全流程；迁移映射在 `ui/README.md` 更新。2026-09-18 完成并整体升级为
  「任务控制台」harness 前端（见修订记录），验收证据 `.impeccable/review/`。
- [x] `M1.5-08` 落地设计 token、基础组件与主题系统：设计规范 §2 三层 token 全量
  进入 `ui/`、StatusBadge 封闭色映射、ActivityCard/ApprovalCard(占位)/StepCard/
  SnapshotFrame(占位)/Composer 等基础组件规格实现；主题机制（`data-theme`/
  `data-mode` 挂载、即时切换、持久化、跟随系统）与 5 套内置主题（晨蓝/冷杉/暖沙/
  抹茶/玄墨，规范 §2.6 清单）；外观设置页接入主题库卡片预览；M1.5-04 既有界面
  样式迁移到 token，组件内私定颜色清零。验收：规范 §2.5 核心组件在 mock 界面
  可见；5 套主题全量覆盖以测试锁定并通过 §2.6 五项门槛；明暗 × 多主题切换无视觉
  断裂、布局零位移；既有一致性测试全绿。

工作项依赖关系：`M1.5-04` 可立即开始（仅依赖 DEC-012 中的消息表草案作 mock 依据）；
`M1.5-01` → `M1.5-02` → `M1.5-05`；`M1.5-03` 依赖 `M1.5-02` 的帧语义定稿（实现可在
其评审期间并行）；`M1.5-06` 随 `M1.5-01` 起逐步接入；`M1.5-08` → `M1.5-07`（先
token 后迁壳），二者以 `M1.5-04` 与 DEC-013 为前置，可与契约线并行。

## 风险与阻塞

- ~~**DEC-012 处于 Proposed**~~：已解除（2026-09-16 评审通过转 Accepted，见
  [DEC-012](../decisions/DEC-012-ipc-event-subscription-and-wire-schema.md) 变更记录）；
  评审未修改契约内容，`M1.5-01`/`M1.5-02` 按原验收标准执行。事件帧格式与订阅语义的
  最终冻结仍以 `M1.5-02` 落地为准。
- **契约漂移**：schema 文档 / C++ / TS 三处一致性靠 golden vectors 测试锁定，漂移在
  CI 可见（`frontend` job 已随 `M1.5-01` 接入）；本地提交前运行双端测试仍是提交纪律。
- **前端工具链未冻结**：Vite/包管理器/框架均为开发期暂定，若 M5 定案改选，迁移范围
  限于 `ui/` 工程文件与 CI job，不触及契约层（`ui/contracts` 为纯 TS + JSON，无框架
  依赖）。
- **dev bridge 的工具定位**：必须保持开发期工具边界（不进安装包、不产生产品承诺）；
  若未来产品化壳内 IPC 桥需要 WebSocket 形态，属 M3 壳 PoC 范畴重新评估，不复用
  dev bridge 的定位。

## 测试与退出条件

- [x] schema 文档与 `runtime/ipc` 现状及事件扩展一致；C++ 与 TS golden vectors 测试
  全绿并已接入 CI（2026-09-16，见验证记录 `M1.5-01`/`M1.5-06` 条目）。
- [x] 协议/服务新增测试覆盖 DEC-012 验证方式所列用例；M1 既有协议测试零回归；预设
  矩阵 `debug` / `release` / `asan` / `ubsan` / `tsan` 通过，`ctest` 无 skip 冒充成功
  （2026-09-17，见验证记录 `M1.5-02` 条目：`event_subscription_test` 15 场景 +
  golden gate 事件断言，五预设 15/15 全绿无 skip，tsan overflow 场景确定性注入
  6/6）。
- [ ] 浏览器经 dev bridge 对真实 `mirage-service` 完成 submit → 实时进度 → cancel →
  终态的可视化闭环（截图/录屏留档为验证记录）；无 Service 时 mock 路径独立可用。
- [ ] 订阅不可用时前端自动降级轮询且功能等价；overflow/重连后 resync 行为正确。
- [ ] 文档同步完成：设计文档第 12 节事件面补记、DEC-012 状态与变更记录、总计划索引
  与本计划验证记录。

## 验证记录

（按日期追加）

2026-09-18：`M1.5-03` 开发期 dev bridge 完成。

- 范围：`apps/devbridge`（可执行 `mirage-devbridge` + 内部库 `mirage_devbridge_core`，
  开发期工具，无安装规则、不进产品包）。`websocket.{hpp,cpp}`：自包含 RFC 6455
  服务端编解码（SHA-1+base64 握手、掩码校验、RSV/opcode/控制帧规则、分片重组、
  ping/pong/close、HTTP 400 拒绝非 WebSocket 探测），零新增第三方依赖；
  `bridge_loop.{hpp,cpp}`：单 poll 驱动 `DevBridgeLoop : executor::IBlockingIoWorker`
  承载全部会话（阻塞 I/O worker 承载双端 I/O），每浏览器会话 1:1 懒连接上游 Unix
  socket（非阻塞 connect），一条 WebSocket binary 消息恰承载一个完整帧、载荷逐字节
  透传（协议文档 §1 已补记该映射）；双方向各自 `max_relay_buffer_bytes` 字节预算，
  越界显式 close（1011），上游断开 1001、协议违规 1002/1003/1009、会话超限 HTTP
  400 拒绝；Session RAII 管理 fd；有序退出时逐会话补发 close 帧并尽力冲刷。
  `main.cpp` 是本进程唯一 Executor owner（EXEC-01 规则 7）：SIGINT/SIGTERM 经
  self-pipe 注册给循环，退出后由主线程回收 worker 并 `shutdown(true)`。
  WebSocket wire 映射注记同步进
  [mirage-ipc-protocol-v1.md](../design/mirage-ipc-protocol-v1.md) §1（非规范）。
- 依据：[DEC-012](../decisions/DEC-012-ipc-event-subscription-and-wire-schema.md)
  决策 6、[DEC-007](../decisions/DEC-007-local-ipc-and-runtime-service.md)（帧格式
  透传不变）、总计划 `EXEC-01`/`EXEC-02`/`RULE-01`/`RULE-07`。
- 验证：Linux x64。测试全部由 Independent-Verification-Agent 独立完成（两轮）：
  - 第一轮：新增 `tests/devbridge/websocket_test.cpp`（编解码 20 场景 206 项检查：
    SHA-1/accept_key RFC 向量、base64、三种长度形态、协议错误矩阵、分片重组、
    握手成功/逐字节/拒绝矩阵、pipeline 字节保留）与
    `tests/devbridge/bridge_relay_test.cpp`（executor + 真实 socket 集成 8 场景：
    帧字节一致透传、顺序性、ping/pong、close 码矩阵、上游缺失 1011、预算触发
    1011、会话准入 400、stop_serving 有序关闭；假上游以 blocking I/O worker
    承载，无自建线程）。该轮发现三个实现缺陷：SHA-1 消息长度编码位移错误（阻断：
    accept 值全错，浏览器必然拒绝握手）、Session fd 泄漏（drop 路径不关 fd，
    close 握手后 TCP 不关闭）、base64 接受纯 padding 输入。
  - 主循环修复后第二轮复验：SHA-1 对照 Python hashlib 多向量一致；fd 计数稳定；
    debug/asan/ubsan/tsan（`setarch -R`）/release 五预设下两测试 206+112 项检查
    全绿、无 sanitizer/竞态报告；debug 全量 ctest 17/17 零回归；
    `mirage-format-check` 通过；进程级 SIGINT 退出码 0 且输出 "stopped cleanly"。
  - 浏览器端到端（ZCode 内置 Chromium 1280x900，截图留档
    `docs/verification/m1.5-03-browser-e2e.png`）：真实 `mirage-service`
    + `mirage-devbridge --port 18787`，页面以 `ui/contracts` 同款 framing 经
    WebSocket 完成 hello（`events:true` 能力通告）→ `events.subscribe`（seed
    `host.status` seq 1）→ `task.submit`（filesystem.read 步骤真实执行）→
    `task.inspect`（`Completed`、步骤结果逐字正确）→ 4 条 `task.updated` 事件
    seq [1..5] 单调 → `events.unsubscribe`，全链 ALL PASS；随后对常驻桥进程
    SIGINT，干净退出（"stopped cleanly"）。验收后所有进程已停止。
- 遗留与边界：上游 mid-frame 半途断开路径无专门用例；`--port`/`--max-sessions`
  仅经进程级冒烟覆盖；浏览器内置真实 transport（订阅实时刷新、降级轮询、
  resync）属 `M1.5-05`；`runtime_service_test` 存在与本变更无关的历史偶发
  （本轮全量与单跑均绿）。

2026-09-17：`M1.5-02` service 事件订阅完成，事件帧语义冻结。

- 范围：`runtime/ipc` 事件编解码（`events.subscribe` / `events.unsubscribe` 请求、
  `Event` 信封与 `task.updated` / `host.status` / `events.overflow` 事件集、
  `ServiceIdentity::events` 能力成员，错误串与 golden vectors 逐字对齐）；
  `runtime/service` 事件承载（`EventHub`：`executor::comm::Topic` 广播 +
  `LatestMailbox` 持最新 host 状态；每连接 drop-oldest 有界队列随订阅下发，
  IPC 循环响应帧优先写出、溢出合成 `events.overflow`、断连销毁订阅排空队列；
  订阅建立先下发当前 host 状态 seed，与前端 mock Enqueue-after-subscribe 一致；
  `ServiceConfig::event_queue_capacity` 显式容量）。发布点：任务创建/取消受理
  （serial 域）与驱动步进/终态（驱动线程 best-effort 投递 serial 域）。TS 镜像
  （`ui/contracts`）同步两个新 op；golden vectors `meta.version` 1 → 2：requests
  增补两条订阅向量，过时的 `unknown-op-events-*`（旧服务端拒绝行为）替换为
  `unknown-op-registry-edit`，`hello-capability` `encode_pending` 解除。
  同一变更顺带修复实现勘察中发现的两个非契约缺陷：驱动 post 到 serial 上下文的
  lambda 按值捕获栈上字符串（原按引用在 serial 积压超时后悬垂，ASan/TSan 可复现
  stack-use-after-return）；`IpcStream::write_some` 改 `send(MSG_NOSIGNAL)`
  （嵌入方未忽略 SIGPIPE 时断连写出不再杀进程）。
- 依据：[DEC-012](../decisions/DEC-012-ipc-event-subscription-and-wire-schema.md)
  决策 2-5、[mirage-ipc-protocol-v1.md](../design/mirage-ipc-protocol-v1.md) §7
  （随本项冻结）、设计文档 §12.2 补记、总计划 `EXEC-02`/`RULE-07`。
- 验证：Linux x64。测试全部由 Independent-Verification-Agent 独立完成（三轮）：
  - 第一轮：golden gate 更新（`ipc_protocol_golden_test` 消费 events/
    event_failures，`hello-capability` 双向断言）+ 新服务级
    `event_subscription_test`（15 场景：hello 能力、订阅/退订 round-trip、seed、
    task.updated 全序列与 seq 连续、未订阅零事件帧、双订阅者扇出、断连清理、
    退订静默、小队列 overflow 注入、响应不被饥饿、未知 op 稳定错误、容量校验）。
    debug/tsan/asan/ubsan/release 五预设 ctest 15/15 全绿（tsan 按 `setarch -R`），
    `npm run check` + vitest 342/342 全绿；篡改实验确认门禁按向量名精确失败、
    还原后复绿。
  - 第二轮：复核两项缺陷修复——UAF 复现场景（ASan/TSan 重载风暴，含修复前
    阳性对照：还原按引用捕获必现 stack-use-after-return）修复后零报告；SIGPIPE
    场景移除 SIG_IGN 屏蔽后服务存活（机制对照：裸 `write` 同条件被杀 exit 141）；
    五预设 ctest 与前端测试零回归。发现 overflow 场景在 TSan 下计时敏感偶发
    假失败。
  - 第三轮：overflow 场景改写为确定性注入（订阅连接不读 + 8 个未订阅连接先写
    全部 task.submit 帧再读响应——loop 线程在单 tick 内串行处理多帧期间不排空
    事件通道，容量 2 的队列必然 drop-oldest 溢出，不依赖墙钟竞争）。tsan 下该
    二进制 6/6 通过、全套 ctest 2 轮 15/15，debug/release/asan/ubsan 均 15/15。
  - CI 接线：首轮 PR CI 暴露两处环境差异——clang-format 违例（已按 v18 套用全部
    触及文件）与 overflow 场景响应收集在 2 核 runner 上超读预算（溢出断言本身
    通过；放宽 burst 响应/终态预算至 60s/240s、ctest timeout 900s）。修复后
    PR CI 7/7 job 全绿（5 预设矩阵 + style + frontend）。
- 遗留与边界：devbridge 端到端（浏览器 WebSocket）属 `M1.5-03`/`M1.5-05`；事件
  的实时性以 IPC 循环 poll 周期（200ms 上限）为界，不构成实时承诺；连接级事件
  容量默认 256，产品化规模（M5）复核。过载病理（既有，非本项引入，待后续工作
  项评估）：serial 积压超过 `command_wait` 时 `begin_operation` 有界等待放弃会把
  已受理任务终态化为 Cancelled（显式、有事件、幂等，但超时是驱动自身预算而非
  pinned 拒绝，语义需结合 DEC-007/DEC-009 复核）；同一过载下 submit 响应可延迟
  至秒级。

2026-09-16：`M1.5-04` 前端工程骨架与 mock 全流程可视化完成。

- 范围：`ui/` 初始化 npm workspaces（`contracts` + `app`；Vite + TypeScript 为开发期
  暂定工具链，组件框架待实现评审定选）。`ui/contracts` 交付协议 v1 wire 契约的
  TypeScript 镜像（消息类型、编解码、4 字节小端 framing、DEC-012 草案的事件信封与
  订阅语义、`EventSequencer` 重同步纪律、`BoundedEventQueue` drop-oldest 队列）与
  mock transport（内存任务状态机 + 事件模拟；host 五态、progress 投影、稳定错误形状
  与真实 M1 服务对齐，`fail:` 注入与 `eventsCapability:false` 降级开关为 mock 专属
  约定，见 `ui/README.md`）。`ui/app` 以 vanilla TS 交付 Workspace（服务身份/主机
  状态/任务提交）、Tasks（任务列表）与 Execution（step 级进度、结果与错误、取消）
  三个视图，事件驱动刷新，实现 seq 跳跃/overflow 触发 resync 与事件不可用时的轮询
  降级。`ui/workspace` 等占位目录与视图模块的映射说明见 `ui/README.md`。
- 依据：[DEC-006](../decisions/DEC-006-ui-web-frontend-packaging.md)、
  [DEC-007](../decisions/DEC-007-local-ipc-and-runtime-service.md)（传输与帧格式冻结）、
  [DEC-012](../decisions/DEC-012-ipc-event-subscription-and-wire-schema.md)
  （Proposed；本工作项按计划仅消费其消息表草案作 mock 依据）；总计划 `EXEC-02`/
  `RULE-01`/`RULE-07`。C++ 端零改动，`RULE-01` 依赖方向不受影响。
- 验证：Linux x64（Node 22.23.2、npm 10.9.8、TypeScript 5.9、Vite 7.3.6、
  Vitest 3.2）：
  - `npm run check`（contracts/app 两包 `tsc --noEmit` strict）通过；`npm run build`
    通过。
  - 测试全部由 Independent-Verification-Agent 独立完成（两轮）：初轮 115/115 通过，
    并以探针测试发现 3 处 mock 边缘缺陷（`events.overflow` 计数失真、`starting`
    态 `shutdown` 被悬挂启动计时器翻回 `running`、关闭后同步抛异常而非 reject）；
    主循环修复后复验新增 4 条回归用例锁定缺陷行为，**119/119 通过、0 失败**，
    strict tsc 全绿。
  - 浏览器验收（Chromium 1280x860，`vite build` + `vite preview`，截图留档
    `docs/verification/m1.5-04/`）：以 mock 数据、无任何后端依赖完成 submit →
    step 级实时进度（pending/running 逐条刷新）→ cancel（Cancelling → Cancelled，
    运行中步骤取消、未开始步骤跳过）→ 终态闭环；Completed 成功横幅、Failed
    fail-fast 与错误视图、任务列表进度徽标、无效任务空态路由均验证正确；缺陷修复
    后重建产物复测通过。验证后预览服务器已关闭。
- 遗留与边界：`M1.5-01`（schema 文档、golden vectors 双端一致性门禁）未开始，仍以
  DEC-012 评审接受为前置；`ui/contracts` 当前与 C++ 端的一致性靠人工对照
  `runtime/ipc/src/protocol.cpp`，golden vectors 门禁落地前两端漂移风险由本记录
  显式承接。

2026-09-16：DEC-012 评审通过，契约线门禁解除。

- 维护者授权评审。按工程规范第 14 节六项重点逐项核查通过（对 pinned executor
  `executor::comm` 公开头、`runtime/ipc` 协议面与错误路径、`ui/contracts` 镜像的
  现状对照），状态 Proposed → Accepted；评审未修改契约内容，`M1.5-01`/`M1.5-02`
  按原验收标准执行。记录见
  [DEC-012](../decisions/DEC-012-ipc-event-subscription-and-wire-schema.md) 变更记录。

2026-09-16：`M1.5-01` 协议 v1 wire schema 事实源与双端一致性门禁完成。

- 范围：新建事实源文档
  [mirage-ipc-protocol-v1.md](../design/mirage-ipc-protocol-v1.md)（传输与帧格式转录、
  信封与帧判别、canonical 序列化规范、请求/响应/事件消息表、错误码表、golden
  vectors 说明、版本与兼容纪律），与 `runtime/ipc` 现状及 DEC-012 事件扩展互审一致；
  共享 golden vectors `tests/runtime/data/ipc_protocol_golden.json`（requests 7、
  request_failures 14、responses 10、response_failures 10、events 4、event_failures 9、
  framing 2、framing_failures 1）；C++ 消费者 `tests/runtime/ipc_protocol_golden_test.cpp`
  与 TS 消费者 `ui/contracts/test/golden-vectors.test.ts` 读取同一向量文件；
  `frontend` CI job 接入 `.github/workflows/ci.yml`（npm ci → strict tsc → vitest →
  build，浅检出、不触及 pinned 子模块）。
- 依据：[DEC-012](../decisions/DEC-012-ipc-event-subscription-and-wire-schema.md)
  （Accepted）决策 1、[DEC-007](../decisions/DEC-007-local-ipc-and-runtime-service.md)
  （传输/帧格式/错误面转录自现状）；`ui/README.md` 的契约入口同步更新。
- 验证：Linux x64（Node 22.23.2、Vitest 3.2、CMake debug 预设）。测试全部由
  Independent-Verification-Agent 独立完成：
  - C++：`ipc_protocol_golden_test` 7 场景 225 项检查 0 失败（双向编码/解码、稳定
    错误串精确与前缀匹配、`hello-capability` 按 `encode_pending` 仅解码并验证未知
    成员容忍）；debug 全量 `ctest` 14/14 通过，M1 测试零回归。
  - TS：新增 58 用例，`ui/` 全量 vitest **341/341 通过、0 失败**；`npm run check`
    （两包 strict tsc）与 `npm run build` 全绿。
  - 门禁有效性经变异测试确认：篡改 vectors 的 canonical/错误串/frame_hex 后，两端
    测试按向量名精确失败，还原（字节级 cmp 确认）后复绿。
  - CI：`frontend` job 已接线并随本工作项 PR 运行通过（7/7 job 全绿：5 预设矩阵 +
    style + frontend；frontend `npm ci → check → test → build` 19s pass）。
- 遗留与边界：`events` / `event_failures` 向量的 C++ 消费与 `hello-capability` 的
  编码方向随 `M1.5-02` 事件编解码落地解除（向量文件中以 `encode_pending` 显式标记，
  无静默跳过）；前端未配置独立 lint 工具，`npm run check` 的 strict tsc 为当前静态
  检查门禁，eslint 等工具链选型属 DEC-006 决策 6（M5）范围。

2026-09-16：`M1.5-06` 前端门禁接入 CI 完成。

- 范围：`.github/workflows/ci.yml` 新增独立 `frontend` job（浅检出，不触及 pinned
  子模块；Node 22；`npm ci` → 两包 strict tsc → vitest 全量含 golden vectors 门禁 →
  vite build）；golden vectors 保持单一事实源（C++ 与 TS 测试读取
  `tests/runtime/data/ipc_protocol_golden.json` 同一文件）；既有 CMake 预设矩阵与
  style job 不变。
- 验证：本工作项 PR CI 7/7 job 全绿（5 预设矩阵 + style + frontend）。
- 边界：仓库未配置独立前端 lint 工具（eslint 等），当前静态检查门禁为 strict tsc
  （`npm run check`）；独立 lint 工具如需引入，随 DEC-006 决策 6 的工具链定案评估。

2026-09-16：`M1.5-08` 设计 token、基础组件与主题系统完成。

- 范围：三层 token 落地 `ui/app`——L1 原始值（`ui/app/src/theme/primitives.ts`，
  单一事实源生成注入 CSS）→ L2 语义层（`schema.ts` 的 `SEMANTIC_TOKENS` 25 项全量
  清单，shadcn 兼容命名 + Mirage 扩展）→ L3 组件变量（`styles.css`）。5 套内置主题
  （晨蓝/冷杉/暖沙/抹茶/玄墨，`themes.ts`，每套 light/dark 完整 L2 值集）经
  `buildThemeStylesheet` 生成 `[data-theme][data-mode]` 规则，`ThemeManager`
  （`theme-manager.ts`）实现挂载、即时切换、localStorage 持久化与跟随系统
  （`prefers-color-scheme` 监听）。组件规格：StatusBadge 封闭映射
  （任务/步骤/主机三状态集 → 六语义色调）、StepCard（执行视图步骤）、ActivityCard、
  ApprovalCard/SnapshotFrame（占位规格，随 DEC-010 权限线与 M2 观察线接线）、
  Composer（工作台任务提交表单迁入执行模式，功能等价）。外观设置页
  （`#/settings/appearance`）提供主题库卡片预览与明暗三态。M1.5-04 既有界面样式
  全量迁移到语义 token，组件与样式私定颜色清零。
- 依据：[DEC-013](../decisions/DEC-013-frontend-ia-harness-first.md) 决策 6、
  设计规范 §2（§2.2 基准值、§2.5 组件、§2.6 主题五项门槛）。状态四色 light 值在
  §2.2 基准上按 §2.6 对比度门槛校准（绿 `#157f3d`、琥珀 `#b2540a` 加深，hue 与
  语义不变），实现于 `themes.ts` 并记入规范 §6 变更记录。
- 验证：Linux x64（Node 22.23.2、Vite 7.3.6、Vitest 3.2）：
  - 测试全部由 Independent-Verification-Agent 独立完成：新增 164 条用例
    （`ui/app/test/`：主题全量覆盖与 CSS 产物、WCAG 对比度门槛 10 组全过、
    状态封闭映射、组件零私定颜色扫描、主题产物零布局属性、外观解析回退、
    ThemeManager 挂载/持久化/跟随系统/stop），合计 **283/283 通过、0 失败**
    （含既有 119 条 contracts 测试）；`npm run check` 与 `npm run build` 全绿。
  - 浏览器验收（Chromium 1280x720，截图留档 `docs/verification/m1.5-08/`）：
    默认 dawn/system 挂载正确；玄墨 × 深色、晨蓝 × 浅色切换即时生效且持久化
    （`data-theme`/`data-mode`/localStorage 三方一致）；主题库 5 卡片与组件占位
    预览可读；Composer 提交（含 `fail:` 失败注入）→ 执行详情 StepCard failed、
    StatusBadge 语义与 fail-fast 横幅全部正确。验证后预览服务器已关闭。
- 遗留与边界：布局零位移以静态等价物锁定（主题产物只含颜色/圆角/阴影自定义
  属性），浏览器内运行时位移对比不在此项范围；`M1.5-07` 统一壳迁移（ActivityBar/
  StatusBar/会话侧栏/路由表）为下一工作项，其视图按规范 §3.3 迁移。

## 修订记录

- 2026-09-16：依据 [DEC-013](../decisions/DEC-013-frontend-ia-harness-first.md)
  增补 `M1.5-07`、`M1.5-08`（统一壳与信息架构重构、设计 token 与基础组件），前端
  目标形态从"Workspace/Tasks/Execution 三视图"修订为"harness 优先统一壳"（会话
  为默认落地页，workflow 一级入口）；`M1.5-04` 能力全部保留并按设计规范 3.3 迁移。
  里程碑目标与退出条件相应扩充：退出条件第 3 条的"可视化闭环"在新 IA 下验收。
- 2026-09-16：依据 DEC-013 决策 6（多主题系统，维护者指示支持多种风格化主题供
  用户选择）扩充 `M1.5-08`：主题机制 + 5 套内置主题 + 外观主题库，验收增补
  规范 §2.6 五项门槛。

- 2026-09-18：`M1.5-07` 完成，并依据维护者的 UI 重写委托整体升级为「任务控制台」
  （Mission Console）harness 前端：React 19 + Base UI 组件栈定选（DEC-014）、
  壁挂屏/灯阵/蓝线剖面视觉世界落地（实现事实源 `DESIGN.md` 与
  `.impeccable/design.json`）、会话/消息/审批/工作流以 mock 先行（设计规范 §4
  前瞻依赖不变）；内置主题扩为 6 套并以「任务控制台」为默认（设计规范 §2.6
  同步）。测试 466 例全绿（`ui` vitest，含 golden vectors 与 style-scan）；
  终审截图 `.impeccable/review/`。
- 2026-09-18（二次）：按维护者反馈修订——会话页确认为纯 agent harness（工作流
  只是会话内可调用的工具：WorkflowCallCard）；工作流页与 harness 同壳（左栏 =
  已保存工作流列表）；编辑器按 RPA 工程范式落地（右栏原子动作库拖入 + 属性/
  参数 Tab；调研 `docs/research/rpa-editor-design-reference.md`）；后端接口缝
  固定为 `WorkflowBackend`（预期 IPC 面 `workflow.*` 已登记设计规范 §4）。
