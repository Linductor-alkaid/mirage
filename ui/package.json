{
  "name": "@mirage/ui-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "contracts",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
